# Task 8 Team Rerun 010 — Parity-corrected proof

**Status:** COMPLETED — STATIC PARITY REVIEW PASSED WITH ONE AGENT MISTAKE; VISUAL REVIEW PENDING  
**Task:** `dse.ai-eval.team-management`  
**Purpose:** re-run Team after prototype-vs-evaluation parity audit corrections  
**Evaluation branch:** `ai-eval/team-management-rerun-010`  
**Verified baseline:** `d7b37a8bc835e31afec5991333c6e8c80cb52df6`  
**Baseline CI:** run 625 — PASS  
**Bootstrap HEAD:** `80c20fdb7c6e7fa6b3c149177a60c513cc0d4330`

Corrections present in baseline:

- P1 — public token CSS consumer contract + unknown governed CSS-reference validation;
- P2 — StatusBadge intrinsic content width;
- P3 — agent-visible Team product-fidelity authority for shell, breadcrumbs, data, detail content, and product-authorized Search width.

Branch integrity:

- merge base equals the verified baseline exactly;
- branch is two commits ahead and zero behind;
- only `TASK-PAYLOAD.json` and `START-HERE.md` differ from baseline.

The run remains blind to denied prototype/answer source.


## Run outcome

Final HEAD: `d3345d78ec508ee9a57254e633ef1f17bb7152b1`

Independent post-push capture verified:

- exactly one implementation commit ahead of bootstrap;
- five changed files;
- all files inside the Team evaluation write root;
- no denied-source/prototype/evaluator contamination signals.

Agent verification reported:

- `pnpm test`: 125 files / 466 tests PASS;
- `pnpm typecheck`: PASS;
- Storybook package production build: PASS;
- root Storybook wrapper was blocked by sandbox `tsx` IPC `EPERM`, while its token/build prerequisites and the package build succeeded;
- clean working tree.

## Parity-correction verification

### P1 — token CSS consumer authority

Resolved in the rerun.

Every `--dse-*` reference in the authored Team TSX/CSS resolves to a real generated token custom property. The invalid Run 009 aliases are gone.

The detail host/panel now uses:

- `--dse-layout-semantic-top-height`;
- `--dse-layout-semantic-detail-available-width`;
- `--dse-elevation-plane-overlay`.

### P2 — StatusBadge intrinsic width

The rerun baseline contains the component-level `inline-size: fit-content` correction. The Team evaluation does not override StatusBadge width.

### P3 — product-fidelity authority

Run 010 includes the now-agent-visible product requirements:

- Overview / Projects / Team & access / Settings;
- Northstar workspace / Signed in as Admin Sidebar footer;
- Northstar / Workspace administration / Amal Hassan · Admin + AH account context;
- Workspace / Team & access Breadcrumbs;
- Team members title/description;
- all seven supplied people/invitations with emails;
- 360 CSS-pixel wide Search in wide presentation;
- Table footer visible-result count;
- member email/detail identity;
- Current saved role;
- descriptive Member/Admin Radio Group options;
- save-effect note;
- Close + Save changes actions.

## Agent mistake M3 — Side Panel close affordance hidden

Run 010 explicitly sets:

`showClose={false}`

No agent-visible product authority asks to hide the close affordance. The Side Panel contract default is `showClose=true`, and the reviewed intended composition includes the governed top-right close button in addition to footer actions.

**Classification:** agent mistake.

No Design System change is justified; the component default and contract already supply the correct behavior.

## Visual-review status

Static parity is substantially improved, but Task 8 must not repeat the prior mistake of declaring visual fidelity from source/build evidence alone.

A human-visible screenshot/render comparison of Run 010's wide directory and member-detail state against the intended product composition is still required before the final visual-fidelity result.
