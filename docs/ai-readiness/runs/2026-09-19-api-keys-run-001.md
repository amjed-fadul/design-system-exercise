# Task 5 Run 001 — API Keys Management

**Status:** INVALID — WORKSPACE NOT MOUNTED  
**Task:** `dse.ai-eval.api-keys-management`  
**Case:** generalization  
**Evaluation branch:** `ai-eval/api-keys-run-001`  
**Approved baseline:** `a2ac1aad833705f332e1ffdc9793d704b9fe7e4a`  
**Bootstrap HEAD:** `c0da60b7f4e8bf6a94867c7b57eb423df6677113`  
**Target agent:** fresh coding-agent session (Codex / GPT-5.6 Sol acceptable)

## Blindness status

This current orchestration chat is contaminated because it has seen the existing prototypes and evaluator-only Task 4 expectations. It must not author the blind implementation.

The isolated branch contains only two bootstrap files inside the task write root:

- `TASK-PAYLOAD.json` — safe projection of `agentVisible` only.
- `START-HERE.md` — blind-run instructions.

The full evaluation task JSON remains outside the agent-visible context and contains evaluator-only material.

## Run rule

The fresh agent must:

- work only on `ai-eval/api-keys-run-001`;
- follow `START-HERE.md`;
- obey the allow/deny context in `packages/contracts/ai/evals/test-pack.json`;
- write only inside `apps/storybook/src/ai-readiness/evaluations/api-keys-management/`;
- produce the required TSX, CSS, Storybook story, and `AUTHORING-REPORT.md`;
- receive no human correction during generation;
- record unresolved decisions rather than guessing.

## Completion evidence to capture

After the fresh agent finishes, record:

- agent/model;
- final branch HEAD;
- files changed;
- tests/typecheck/Storybook build result;
- authoring report;
- screenshots/Storybook evidence;
- no-contamination confirmation.

Do not classify failures in this run record. Failure classification belongs to Task 6.


## Run outcome

The fresh coding-agent session opened an empty Git repository instead of the target project.

Observed by the agent:

- active branch was `master`;
- repository had no commits;
- no remote was configured;
- target branch `ai-eval/api-keys-run-001` was absent;
- `START-HERE.md`, `TASK-PAYLOAD.json`, and `test-pack.json` were unavailable;
- no repository files were modified;
- no tests were run;
- no commit was created.

**Scoring:** none. This is an environment/setup failure, not an AI-readiness result and must not enter Task 6 failure classification.

A fresh Run 002 is required with the actual GitHub repository mounted or cloned before the blind prompt is given.
