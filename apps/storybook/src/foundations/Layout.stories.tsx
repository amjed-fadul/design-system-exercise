import type { Meta, StoryObj } from '@storybook/react-vite'; import { tokens } from '@design-system-exercise/tokens'; import { flattenTokens,TokenTable } from '../shared/TokenTable'; const meta={title:'Foundations/Layout'} satisfies Meta; export default meta; type Story=StoryObj<typeof meta>; export const Modes:Story={render:()=> <div className="two-column"><TokenTable rows={flattenTokens('layout.semantic.wide',tokens.layout.semantic.wide)}/><TokenTable rows={flattenTokens('layout.semantic.narrow',tokens.layout.semantic.narrow)}/></div>};
