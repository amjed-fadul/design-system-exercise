import type { Meta, StoryObj } from '@storybook/react-vite'; import { tokens } from '@design-system-exercise/tokens'; import { flattenTokens,TokenTable } from '../shared/TokenTable'; const meta={title:'Foundations/Icons'} satisfies Meta; export default meta; type Story=StoryObj<typeof meta>; export const Foundations:Story={render:()=> <TokenTable rows={flattenTokens('icons',tokens.icons).map((row)=>({...row,preview:row.name.includes('.size.')?<span className="icon-box" style={{width:row.value,height:row.value}}/>:<span className="icon-stroke" style={{borderTopWidth:row.value}}/>}))}/>};
