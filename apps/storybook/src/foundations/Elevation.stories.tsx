import type { Meta, StoryObj } from '@storybook/react-vite'; import { tokens } from '@design-system-exercise/tokens'; const meta={title:'Foundations/Elevation'} satisfies Meta; export default meta; type Story=StoryObj<typeof meta>; export const Planes:Story={render:()=> <div className="elevation-stack">{Object.entries(tokens.elevation.plane).map(([name,value])=><div key={name} className="elevation-plane" style={{transform:`translateY(-${value*2}px)`}}><code>{`elevation.plane.${name}`} = {value}</code></div>)}</div>};
